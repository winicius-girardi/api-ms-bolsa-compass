
spring:
  mvc:
    format:
      date-time: iso
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: true
  datasource:
    url: jdbc:mysql://localhost:3306/ms_address?useTimezone=true&serverTimezone=UTC
    username: root
    password: 123456

  application:
    name: msaddress
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 8081

mq:
  queue:
    ms-address: ms-address

